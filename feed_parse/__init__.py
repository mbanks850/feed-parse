"""feed_parse: convert Rocksmith 2014 CDLC .psarc archives into feedpak v1 packages.

Public entry point: ``feed_parse.cli.main``. The CLI command is registered as
``feed-parse`` by ``pyproject.toml``. See :mod:`feed_parse.convert` for the
high level orchestration, or the package README for usage examples.
"""

__version__ = "0.1.0"
